
import com.hedera.hashgraph.sdk.Client;
import com.hedera.hashgraph.sdk.HederaPreCheckStatusException;
import com.hedera.hashgraph.sdk.HederaReceiptStatusException;
import com.hedera.hashgraph.sdk.TransactionResponse;

import com.hedera.hashgraph.sdk.proto.Transaction;
import com.newjerseysoftware.hederaDemo.model.User;
//import org.junit.Test;
import org.junit.jupiter.api.DynamicTest;

import java.io.IOException;
import java.util.concurrent.TimeoutException;

public class test1{
   /* @DynamicTest
    public void CheckTransaction() throws HederaReceiptStatusException, TimeoutException, HederaPreCheckStatusException, IOException {
        Client client= Client.forTestnet();
        //Vendor vendor=new Vendor("Naxatraz");
        User user=new User("nahara");

        //System.out.println(user.getAccountId());
        int fee=10;
        Transaction tr= new Transaction(user, //vendor, fee);
        //tr.transactionUserVendor();
        //System.out.println(tr.transaction.getReceipt(client).toString());
    }*/
}

